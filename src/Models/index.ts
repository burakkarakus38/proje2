/**
 * Models katmanı: Prisma şemaları ve veri yapıları bu klasörde tutulur.
 * Prisma Client instance'ı Config/database.ts'den import edilir.
 */

import prisma from '../Config/database';

export default prisma;
